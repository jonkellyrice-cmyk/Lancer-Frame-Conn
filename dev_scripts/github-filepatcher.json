{
  "schema_version": 2,
  "id": "frame-helm-sidebar-foundry-v13-lifecycle",
  "description": "Use Foundry v13 ApplicationV2 rendering to attach the sidebar surface and defer reading the client presentation setting until ready.",
  "policy": {
    "max_files_changed": 2,
    "allowed_paths": [
      "styles/ui_application/ui-application.js",
      "scripts/runtime-orchestrator.js"
    ]
  },
  "operations": [
    {
      "type": "replace_text",
      "path": "styles/ui_application/ui-application.js",
      "search": "      renderSidebar:\n        handleFrameConnSidebarRender",
      "replace": "      renderApplicationV2:\n        handleFrameConnSidebarRender",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/runtime-orchestrator.js",
      "search": "    frameConnFoundryIntegrationApi\n      .registerSettings?.();\n\n    void frameConnApplicationApi\n      .setPresentationMode?.(\n        frameConnFoundryIntegrationApi\n          .getPresentationMode?.() ??\n        \"window\"\n      );\n\n\n    /**\n     * Actions retain synchronous catalog initialization",
      "replace": "    frameConnFoundryIntegrationApi\n      .registerSettings?.();\n\n\n    /**\n     * Actions retain synchronous catalog initialization",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/runtime-orchestrator.js",
      "search": "Hooks.once(\n  \"ready\",\n  async () => {\n    frameConnElevationLosApi.ready?.();",
      "replace": "Hooks.once(\n  \"ready\",\n  async () => {\n    await frameConnApplicationApi\n      .setPresentationMode?.(\n        frameConnFoundryIntegrationApi\n          .getPresentationMode?.() ??\n        \"window\"\n      );\n\n    frameConnElevationLosApi.ready?.();",
      "expected_occurrences": 1
    }
  ]
}