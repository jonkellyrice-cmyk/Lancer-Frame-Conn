# Status and Conditions

This directory is the home for non-player-facing Frame Conn rules features that observe, coordinate, or present status and condition behavior.

Native Foundry Lancer remains authoritative for native status/condition execution and document mutation. Player-facing actions that directly add or remove a status or condition remain with the corresponding action feature under `scripts/player_features/`; shared or reactive status/condition behavior belongs here.
