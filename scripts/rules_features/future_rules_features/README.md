# Future Rules Features

Reserved staging area for future reactive/non-player-facing rules feature domains while the architecture is being built out.

When a rules domain becomes concrete, prefer promoting it to a descriptively named sibling directory under `scripts/rules_features/` and register its feature definitions through `rules-feature-registry.js`.
