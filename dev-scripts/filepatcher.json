{
  "schema_version": 2,
  "description": "Implement Brace end to end as a contextual Frame Conn reaction over native Lancer damage and attack-flow boundaries, with actor-keyed reaction tracking and next-turn restrictions.",
  "planning_goal": "implement Brace end to end from a qualifying incoming event through Frame Conn reaction availability and spend, event-bound Brace state, native adapter damage mitigation using Lancer's authoritative half-damage/damageCalc pathway, and presentation/runtime updates without inventing a native Brace flow",
  "id": "implement-brace-reaction-end-to-end-phase-1c",
  "policy": {
    "max_files_changed": 3
  },
  "operations": [
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "  state.assertTurnActive();\n\n\n  const frameConnActionRegistry =",
      "replace": "  state.assertTurnActive();\n\n\n  if (\n    forceOvercharge &&\n    state.restrictions?.brace\n  ) {\n    return {\n      committed:\n        false,\n\n      reason:\n        \"Brace prevents Overcharge until the end of this turn.\"\n    };\n  }\n\n\n  const frameConnActionRegistry =",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "  const standardUsed =\n    Math.min(\n      total,\n      speed\n    );",
      "replace": "  const standardAllowance =\n    state.restrictions?.brace\n      ? 0\n      : speed;\n\n\n  const standardUsed =\n    Math.min(\n      total,\n      standardAllowance\n    );",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "          Math.max(\n            total -\n              speed,\n            0\n          ),",
      "replace": "          Math.max(\n            total -\n              standardAllowance,\n            0\n          ),",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "  const overchargeStart =\n    speed *\n    (\n      1 +\n      normalBoostCount\n    );",
      "replace": "  const overchargeStart =\n    standardAllowance +\n    speed *\n      normalBoostCount;",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "  const legalAllowanceCount =\n    1 +\n    normalBoostCount +\n    overchargeBoostCount;\n\n\n  const legalMaximum =\n    speed *\n    legalAllowanceCount;",
      "replace": "  const legalMaximum =\n    standardAllowance +\n    speed *\n      (\n        normalBoostCount +\n        overchargeBoostCount\n      );",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "    normalBoostCount > 0 &&\n    total > speed",
      "replace": "    normalBoostCount > 0 &&\n    total > standardAllowance",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "  const previousBoostCount =\n    getFrameConnTurnStateMovementBoostCount(\n      state\n    );\n\n\n  const automaticActions =",
      "replace": "  const previousBoostCount =\n    getFrameConnTurnStateMovementBoostCount(\n      state\n    );\n\n\n  const standardAllowance =\n    state.restrictions?.brace\n      ? 0\n      : speed;\n\n\n  const automaticActions =",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "    newTotal > speed &&\n    getFrameConnTurnStateMovementBoostCount(",
      "replace": "    newTotal > standardAllowance &&\n    getFrameConnTurnStateMovementBoostCount(",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "      threshold:\n        speed,",
      "replace": "      threshold:\n        standardAllowance,",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "    newTotal >\n      speed * 2 &&",
      "replace": "    newTotal >\n      standardAllowance +\n        speed &&",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_turn/turn-state-movement.js",
      "search": "      threshold:\n        speed * 2,",
      "replace": "      threshold:\n        standardAllowance +\n          speed,",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_actions/actions-feature.js",
      "search": "        icon:\n          \"fas fa-shield-halved\"\n      }),",
      "replace": "        icon:\n          \"fas fa-shield-halved\",\n\n        metadata: {\n          contextualReaction:\n            true,\n\n          trigger:\n            \"hit-after-damage-roll\"\n        }\n      }),",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "scripts/feature_actions/action-execution-feature.js",
      "search": "      \"full.mount-dismount\",\n\n      \"special.end-turn\"",
      "replace": "      \"full.mount-dismount\",\n\n      \"reaction.brace\",\n\n      \"special.end-turn\"",
      "expected_occurrences": 1
    }
  ]
}
