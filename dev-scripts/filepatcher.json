{
  "schema_version": 2,
  "id": "fix-runtime-contract-probe-clause-normalizer",
  "description": "Correct the malformed spread token in Runtime Contract Probes clause concern normalization so the newly trusted self-test can execute.",
  "policy": {
    "max_files_changed": 1,
    "allowed_paths": ["dev_scripts/runtime-contract-probes.mjs"]
  },
  "operations": [
    {
      "type": "replace_text",
      "path": "dev_scripts/runtime-contract-probes.mjs",
      "search": "function clauses(c){return(c.clauses??[]).map((x,i)=>({id:x.id??`clause-${i+1}`,text:x.text??String(x),concerns:[...new Set([..((x.concerns??[]),...((x.obligations??[]).map(y=>y.concern).filter(Boolean))])]}));}",
      "replace": "function clauses(c){return(c.clauses??[]).map((x,i)=>({id:x.id??`clause-${i+1}`,text:x.text??String(x),concerns:[...new Set([...(x.concerns??[]),...((x.obligations??[]).map(y=>y.concern).filter(Boolean))])]}));}",
      "expected_occurrences": 1
    }
  ]
}
