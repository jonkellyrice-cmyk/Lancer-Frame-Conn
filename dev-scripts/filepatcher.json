{
  "schema_version": 2,
  "id": "validate-automatic-patch-staging-scan",
  "description": "No-op proof that planning_goal automatically proposes dependency-ordered staged FilePatcher specs for the real Scan corridor.",
  "planning_goal": "wire committed Scan through the authoritative native Lancer execution entrypoint and show the Scan choice in the Frame Conn UI",
  "policy": {
    "max_files_changed": 1
  },
  "operations": []
}
