{
  "schema_version": 2,
  "id": "execute-disengage-through-status-orchestration",
  "description": "Implement Disengage as a canonical non-roll action which clears Engaged and suppresses derived reapplication through the end of the current turn.",
  "policy": {"max_files_changed":1,"allowed_paths":["scripts/runtime-orchestrator.js"]},
  "operations": [
    {
      "type":"replace_text",
      "path":"scripts/runtime-orchestrator.js",
      "search":"    case \"lock-on\": {",
      "replace":"    case \"disengage\": {\n      transaction =\n        await frameConnExecutionTransactionApi\n          .runNativeExecutionTransactionWithGlobalHooks({\n            context: executionContext,\n            execute: async () =>\n              frameConnStatusOrchestrationApi.applyDisengage(actor),\n            metadata: {\n              actionId: action.id,\n              executionKind,\n              source: \"action-execution\",\n              removesStatusId: \"engaged\",\n              suppression: \"through-end-of-current-turn\"\n            }\n          });\n      break;\n    }\n\n    case \"lock-on\": {",
      "expected_occurrences":1
    }
  ]
}
