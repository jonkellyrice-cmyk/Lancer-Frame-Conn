{
  "schema_version": 2,
  "id": "validate-frame-conn-internal-migration",
  "description": "No-op validation run after the Frame Conn internal identity migration. The GitHub FilePatcher workflow now executes the repository integrity auditor, which validates relative JavaScript/CSS imports, named exports, feature graph integrity, and module entrypoints.",
  "policy": {
    "max_files_changed": 1
  },
  "operations": []
}
