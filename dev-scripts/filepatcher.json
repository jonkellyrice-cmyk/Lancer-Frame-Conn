{
  "schema_version": 2,
  "id": "exclude-recovery-history-from-change-propagation",
  "description": "Exclude FilePatcher backup and patch-history trees from live change-propagation consumer evidence.",
  "policy": {
    "max_files_changed": 1,
    "allowed_paths": ["dev_scripts/change-propagation-simulator.mjs"]
  },
  "operations": [
    {
      "type": "replace_text",
      "path": "dev_scripts/change-propagation-simulator.mjs",
      "search": "function repositoryFiles() {\n  const out = [];\n  const skip = new Set([\".git\",\"node_modules\",\".next\",\"dist\",\"coverage\"]);\n  function visit(dir) {\n    for (const entry of fs.readdirSync(dir, { withFileTypes:true })) {",
      "replace": "function repositoryFiles() {\n  const out = [];\n  const skip = new Set([\".git\",\"node_modules\",\".next\",\"dist\",\"coverage\"]);\n  const excludedRecoveryTrees = [\"dev_scripts/backups\", \"dev_scripts/patch-history\"];\n  function visit(dir) {\n    const relativeDir = path.relative(ROOT, dir).split(path.sep).join(\"/\");\n    if (excludedRecoveryTrees.some((tree) => relativeDir === tree || relativeDir.startsWith(`${tree}/`))) return;\n    for (const entry of fs.readdirSync(dir, { withFileTypes:true })) {"
    }
  ]
}
