{
  "schema_version": 2,
  "id": "expose-corridor-context-pack-during-planning",
  "description": "Print exact certified context slices during FilePatcher planning_goal runs so remote workflow logs can serve as the authoring context pack.",
  "policy": {
    "max_files_changed": 2
  },
  "operations": [
    {
      "type": "replace_text",
      "path": "dev_scripts/corridor-context-pack.mjs",
      "search": "  const out = { goal:null, corridor:null, output:path.join(ROOT,\"corridor-context-pack.json\"), maxSlices:24, selfTest:false };",
      "replace": "  const out = { goal:null, corridor:null, output:path.join(ROOT,\"corridor-context-pack.json\"), maxSlices:24, selfTest:false, printSource:false };",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "dev_scripts/corridor-context-pack.mjs",
      "search": "    else if (a===\"--self-test\") out.selfTest=true;\n    else if (a===\"--help\"||a===\"-h\") {",
      "replace": "    else if (a===\"--self-test\") out.selfTest=true;\n    else if (a===\"--print-source\") out.printSource=true;\n    else if (a===\"--help\"||a===\"-h\") {",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "dev_scripts/corridor-context-pack.mjs",
      "search": "  if(clauseIds.length)console.log(`    ownership=${slice.family}; clauses=${clauseIds.join(\",\")}`);\n}",
      "replace": "  if(clauseIds.length)console.log(`    ownership=${slice.family}; clauses=${clauseIds.join(\",\")}`);\n  if(options.printSource) {\n    console.log(`----- CONTEXT SLICE ${slice.file}:${slice.symbol.lineStart}-${slice.symbol.lineEnd} ${slice.symbol.name} -----`);\n    console.log(slice.source);\n    console.log(`----- END CONTEXT SLICE ${slice.symbol.name} -----`);\n  }\n}",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "dev-scripts/github-filepatcher.mjs",
      "search": "function reportCorridorScope(plan, corridorReport) {",
      "replace": "function runCorridorContextPack(corridorReport) {\n  const contextScript = path.join(ROOT, \"dev_scripts\", \"corridor-context-pack.mjs\");\n  if (!fs.existsSync(contextScript)) fail(`Corridor Context Pack not found: ${contextScript}`);\n\n  const corridorFile = path.join(os.tmpdir(), `frame-conn-certified-corridor-${process.pid}.json`);\n  const outputFile = path.join(os.tmpdir(), `frame-conn-corridor-context-${process.pid}.json`);\n  fs.writeFileSync(corridorFile, `${JSON.stringify(corridorReport, null, 2)}\\n`, \"utf8\");\n\n  const result = spawnSync(\n    process.execPath,\n    [contextScript, \"--corridor\", corridorFile, \"--output\", outputFile, \"--max-slices\", \"12\", \"--print-source\"],\n    { cwd: ROOT, encoding: \"utf8\", maxBuffer: 8 * 1024 * 1024 }\n  );\n\n  if (result.stdout) process.stdout.write(result.stdout);\n  if (result.stderr) process.stderr.write(result.stderr);\n  if (result.error) fail(`Corridor Context Pack could not start: ${result.error}`);\n  if (result.status !== 0) fail(`Corridor Context Pack failed with exit code ${result.status}.`);\n\n  try {\n    return JSON.parse(fs.readFileSync(outputFile, \"utf8\"));\n  } catch (error) {\n    fail(`Corridor Context Pack report could not be read: ${error}`);\n  }\n}\n\nfunction reportCorridorScope(plan, corridorReport) {",
      "expected_occurrences": 1
    },
    {
      "type": "replace_text",
      "path": "dev-scripts/github-filepatcher.mjs",
      "search": "    const corridorReport = patch.planningGoal\n      ? runPatchCorridorPlanner(patch.planningGoal)\n      : null;\n\n    console.log(`[github-filepatcher] patch=${patch.id ?? \"unnamed\"}`);",
      "replace": "    const corridorReport = patch.planningGoal\n      ? runPatchCorridorPlanner(patch.planningGoal)\n      : null;\n    if (corridorReport) runCorridorContextPack(corridorReport);\n\n    console.log(`[github-filepatcher] patch=${patch.id ?? \"unnamed\"}`);",
      "expected_occurrences": 1
    }
  ]
}
